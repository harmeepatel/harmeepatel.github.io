package pages;

import (
    "iter"
	"harmeepatel.dev/web"
	"harmeepatel.dev/web/layouts"
	"strconv"
	"slices"
)

var ao = 0

css animOrder(o int) {
	--anim-order: { strconv.Itoa(o) };
}

templ Blog(title string, blogList iter.Seq[string]) {
	@layouts.Base(title) {
		<main id="blog-list" class="max-w-screen-sm h-vh mx-6 sm:mx-12 md:m-auto mt-4 sm:mt-6">
			for i, bName:= range slices.Collect(blogList) {
				{{	
                    ao++
                    mt8 := ""
                    fixedTitle := web.GetFromKey(web.Title, bName)
				}}
				if i == 0 {
					{{ mt8 += "mt-4 md:mt-8" }}
				}
				<div class={ "blog-li mb-6 md:mb-8 list-anim " + mt8, animOrder(ao) }>
                    <time>{ web.GetFromKey(web.Date, bName) }</time>
					<a href={ templ.SafeURL("blog/" + bName + ".html") } class="blog-li-a" aria-label={ fixedTitle }>
						{ fixedTitle }
					</a>
				</div>
			}
		</main>
	}
}
